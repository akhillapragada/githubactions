name: Path Filter Workflow

on:
  push:
    paths:
      - 'src/**'
      - 'docs/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Path Filter
        run: echo "Changes detected in specified paths."
