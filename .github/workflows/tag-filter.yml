name: Tag Filter Workflow

on:
  push:
    tags:
      - 'v*' # For version tags like v1.0.0, v2.0.1, etc.

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Run Tag Filter
        run: echo "Tag-based push event detected."

    
